# Foundry FM 1.1.0 — Patch Drag & Drop

À copier dans le dépôt `Saurusius/foundry-fm`.

## Fichiers à ajouter

- `scripts/dragdrop.mjs`
- `styles/foundry-fm-dragdrop.css`

## Fichiers à remplacer

- `module.json`
- `CHANGELOG.md`

Ne remplace pas `scripts/main.mjs` ni `styles/foundry-fm.css`.

Après copie, la branche `main` contient la version source 1.1.0. Le champ `download` de `module.json` cible une future release GitHub `v1.1.0` avec l'archive `foundry-fm-v1.1.0.zip`; il ne sera donc fonctionnel qu'après création de cette release.
